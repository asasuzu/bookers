<h1>Books</h1>

<%= form_with model: Book.new, url: '/books', method: :post do |f| %>

<h2>New book</h2>
  <label>Title</label>
  <%= f.text_field :title %>
  
  <label>Body</label>
  <%= f.text_area :body %>
  
  <%= f.submit '投稿' %>
  
<% end %>

